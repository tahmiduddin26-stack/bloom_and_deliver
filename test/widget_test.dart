// Bloom & Deliver — placeholder widget test.
// The default Flutter counter test was removed because this app does not use
// a counter widget. Add real integration tests here as the game grows.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — no tests yet', () {
    expect(true, isTrue);
  });
}
